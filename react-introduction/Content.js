 function Content({paragraph}){
    return (
        <p className="cursor-pointer p-10 bg-stone-50 flex flex-col rounded-lg hover:scale-105 transition-all shadow-lg text-center m-5">{paragraph}</p>
    );
}